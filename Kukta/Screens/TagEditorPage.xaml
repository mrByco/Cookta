<Page
    x:Class="Kukta.Screens.TagEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kukta.Screens"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tag="using:Cooktapi.Food"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded">

    <Grid>
        <ScrollViewer>
            <StackPanel>
                <Button Content="+ Új tag" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <TreeView x:Name="TreeViewer" ItemsSource="{x:Bind ViewItems}" DragItemsCompleted="TreeViewer_DragItemsCompleted">
                    <TreeView.ItemTemplate>
                        <DataTemplate x:DataType="tag:Tag">
                            <StackPanel>
                                <TextBlock Text="{x:Bind Name}"/>
                            </StackPanel>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
